[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies.rocket]
version = "0.5.0-rc.1"
features = ["json"]

[dependencies]
fractal-simulations = {path = "../fractal-simulations"}

